program lab1
var y, dx, a, b, c : real;
begin
read(a);
read(b);
read(c);
y := 4;
dx := (1 + a) * (b - c);
a := 0.1;
do c := c + dx by a to b;
read(dx);
if not [c > 0] and b > 0 goto 1;
1: y := a + b - c;
write(y);
end